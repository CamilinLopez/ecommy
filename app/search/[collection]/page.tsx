export default async function CategoryPage({
  params,
  searchParams,
}: {
  params: {
    collection: string;
  };
  searchParams?: { [key: string]: string | string[] | undefined };
}) {
  return (
    <section>
      <h1>hola</h1>
    </section>
  );
}
